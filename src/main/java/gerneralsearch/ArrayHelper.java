package gerneralsearch;

public class ArrayHelper {

	public void iterateArrayAndExecute(Pawn[][] pawnMatrix, OptionExecutor optionExecutor) {
		for (int c = 0; c <= Simulator.ROWS; c++) {
			for (int r = 0; r <= Simulator.COLS; r++) {
                 
			}
		}
	}
}
